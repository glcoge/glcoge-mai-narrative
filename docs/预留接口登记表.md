# 预留接口登记表

> 制度来源：ADR-0001 §7。`habits`/`shared_secrets`/`user_notes` 三个死字段的教训——没有登记制度的预留必然烂掉。
> 规则：**任何预留接口/字段/待观察参数必须在此登记**；代码处用 `# RESERVED(Rn)`（未实现预留）或 `# OBSERVE(Rn)`（已实现待观察）注释锚定；调用或删除前先查本表；每批收尾更新状态列。
> 类型：预留=声明了未实现 ｜ 待观察=已实现需定标/验证 ｜ 临时=明确将删。

| ID | 名称 | 位置 | 类型 | 用途 | 启用条件 | 删除条件 | 状态 |
|---|---|---|---|---|---|---|---|
| R1 | `[learned].style` 配置区块 + render 预留槽 | config.toml `[learned]` **原始区块**（❗不声明进 `config.py`，ADR-0002 决策 6）+ `learning/projection.py` 读写层 + `render/replyer_block.py` 注入槽 | ✅ **批 3 落槽** | LLM 风格提炼产物的投影槽（v1 恒空） | 二期引入风格提炼（须齐 ADR-0003 四件套） | 观察期结束仍未启用 → v0.3 删除 | **批 3 落槽**：tomlkit 直读写 + `/narrative status` 展示 + 防重入闸齐备；**写入端（批 4 晋升机）未接** |
| R2 | 群聊三轨接口 | `render/track.py` | 预留 | 寒暄接梗→表达库 / 剧本生活→漂移层 / 平时混合 的分类器挂点 | 用户宣布铺群解冻 | 群聊方案废弃 | ✅ 占位已建（批 0，冻结中） |
| R3 | 漂移风格 LLM 周度提炼 | `learning/drift_style.py` 二期分支 | 预留 | 原文连续性不足时补长期风格漂移 | 实机观察证明原文连续性不足 + 四件套齐上 | 决定永不做 → 删接口 | 未开工 |
| R4 | 晋升门槛数值：minor(0.82/3场景/2天/72h)、major(0.95/2场景，默认关) | config + `state/continuity.py` | ✅ **批 4 定标保留原值** | 照抄 HDS 实测值，需回放台用 26 天真实数据定标 | 批 4 回放定标完成 → 固化为默认 | — | **批 4 定标结论（归档实测）**：77 条合法素材 / 22 个跨日场景（`scene_count=22`、`day_count=22`）→ 冷却窗口内可发生 **8 次晋升**（上界 9）。即**门槛不是瓶颈、冷却是**：真实数据量下 minor 门槛（0.82/3/2）**偏松**，故**不收紧**（收紧只会让功能更难触发，见 HDSI 5.9）。真正决定频率的是 `cooldown_hours=72`。用例：回放台 `promotion_rhythm` |
| R5 | 场景计数映射（1 生活片段=1 场景；1 互动日=1 场景） | `state/continuity.py` | ✅ **批 4 验证成立** | 晋升证据的计数单位 | 同 R4（若永不触发晋升则改映射） | — | **批 4 验证**：归档 77 条合法素材落 22 个自然日 → `scene_stats` 得 (22, 22)（键＝(日,来源)，归档无 `source_uid` 故每源每天 1 场景），**与素材条数解耦**——正是 §0.4 结论 2「同日多条只算 1 场景」的实证。⚠️ 已知口径：归档 export 无运行时生活事件，故按「每天至多 1 个生活场景」取**保守下界**。用例：`promotion_rhythm` |
| R6 | 关系内部证据计数（原 familiarity 语义降级） | branch state | 待观察 | 晋升证据链的确定性时间戳来源 | 晋升机稳定消费它 | 一个月运行后晋升机不消费 → 删 | ✅ **批 2 引入**（`record_branch_feedback` → `state.interaction_count` 纯计数 + `last_interaction_ts`；计数**不进注入块、不进 status**） |
| R7 | 指标 4-B 产出多样性算法（embedding vs 字符 n-gram） | `state/snapshot.py`/telemetry | 待观察 | 铺群闸门的判据来源 | ✅ **已选型（2026-09-25 用户按推荐拍板）：字符 n-gram，n=3~5，去标点与空白后计算**；embedding 留作对照不落地 | — | ✅ 批 1 实现（`snapshot.ngram_diversity`，参数见调参表 P5） |
| R8 | 铺群闸门基线值（指标 4-B ≥ ?×基线） | 本表 + roadmap | 待观察 | 替代旧"指标4≥2x"的无数据判据 | 批 1 回放数据出基线 | — | 待批 1 |
| R9 | `is_command` 本地正则判定 | `plugin.py` 入站 hook | 临时 | 绕过宿主 after_process 时序 bug（命令文本进素材/刷关系值） | 已启用 | 宿主修复置位时序（跟踪 upstream）→ 删正则回读宿主标志 | ✅ 批 1 实现（`message.looks_like_command`） |
| R10 | creator HTTP 直连兜底 | `creation/life.py` | 临时 | 宿主 1.2.0 吞 `model_name`（issue #2031）的绕行 | 已启用 | **✅ 已验证（2026-09-25 容器 1.2.5）**：`_resolve_llm_capability_route` 已透传 model_name（core.py:44-45/616-623）→ 批 1 切回宿主路由并删直连 | ✅ **批 1 已删除**：直连分支 + `[creator_model]` 段（含明文 api_key）已删，`max_tokens` 迁到 `[llm].creation_max_tokens`；`test_config_schema` 有「不得再出现 api_key/base_url 字段」反向护栏防复活 |
| R11 | 表达学习双阻断 hook（select/learn） | `plugin.py:445/465` | 待观察 | 防群腔调进私聊剧本、防剧本素材进表达库 | 已启用 | 群聊三轨实现时整体重估 | 保留 |
| R12 | `schema_version` + store 迁移入口 | `store.py` | 待观察 | **state 结构级**的版本化迁移挂点（与 R21 的表级迁移区分） | 下一次 state 结构变更 | — | ✅ **批 2 落地**（`STATE_SCHEMA_VERSION=2` 落在 self/branch 的 `meta.version`；开库版本不符 → WARN + 原地重置，chronicle 保留；`reset_state()` 重建带版本默认态防误判旧库） |
| R13 | diary append 接口 audience 参数 | `narrative_chronicle_append` API | 预留 | diary 侧未来打标推送的协议位（当前 diary 产物完全隔离，参数先收后忽略） | diary 仓适配 | diary 依赖废弃 | ✅ 批 1 加参（`audience: str \| None = None`，None→`source=diary`） |
| R14 | 锚定一致性启动 WARN | `plugin.py` on_load | 待观察 | 宿主 personality vs 插件 world 双人格检测（LLM 一次性比对） | 已设计 | 误报率高到无用 → 降为手动命令 | ✅ **批 2 实现**（`plugin._check_anchor_consistency`，配置项 `[anchor].consistency_check` **默认关**；不一致仅 WARN 不阻断；任何异常静默降级为 debug） |
| R15 | 慢变 seed 初始值（`origin=seed`） | `state/continuity.py` + config | ✅ **批 4 落地** | 冷启动：world_view/life_goals 的晋升 diff 基线（ADR-0002「冷启动」） | 批 4 实现 | seed 长期未被任何晋升取代 → 查锚定派生质量或调派生 prompt | **批 4 落地**：`seed_perspective` 从 `[identity].world/values` 低温派生，标 `origin=seed`；**幂等**（origin 或两维有内容即 `already`，不重复调 LLM）；失败/解析失败**不阻断启动**（留空 + WARN，下次启动再试）；配置 `[promotion].seed_on_start`。用例：`test_slow_seed.py`（21） |
| R16 | 话题偏好外部加权系数（用户接话 vs 生活片段来源） | `learning/topic.py`（批 3 建）+ `proactive/scheduler.py` 两处证据落点 | ✅ **批 3 引入** | 防自主信息茧房（ADR-0003 §7） | 批 3 实现，系数凭回放/实机观察调 | — | **批 3 引入**：话题 = 文本归一化前 12 字签名（`topic_key`）；接话 1.0 / 自身片段 0.4（P10）。<br>⚠️ **已知局限**：表述差异大的同一话题不聚合（零 LLM 的代价）；**消费端未接**（主动性选材仍不读权重） |
| R17 | 晋升计数器四指标（提案/晋升/反证/回滚） | telemetry 通道 + `state/continuity.py` | ✅ **批 4 四路全通** | 晋升频率真实数据（HDSI 盲区一年的空白）；告警线待实机数据定 | 批 1 建通道 / 批 4 写入 | — | ✅ 批 1 建通道（`Telemetry.record_counter`，四种 kind 各落一个 csv；未登记 kind 抛错）；**✅ 批 4 写入**：提案落库 / 晋升成功 / 反证命中 / 回滚执行四处埋点 → 端到端验证真的落 `metrics/{kind}.csv`（不只是内存计数）。⚠️ **已知耦合**：`record` 受 `[telemetry].enabled` 门控 → **关掉验收采样 = 关掉晋升链路的全部可观测性**（配置注释已写明 + 读到 0 条时 WARN 限频）。用例：`test_telemetry.py`（13） |

| R18 | diary 对外 API 的 chronicle 输出口径 | `narrative_diary_context` API（`plugin.py:720-724`） | 待观察 | narrative → diary 的 context 面；下游＝**公开的 QQ 动态**。<br>**用户 2026-09-25 裁决：Q1 选 (b) —— 维持现状（继续给 `recent_chronicle` 原文），后续视情况再优化** | 出现「涉私原文经 diary 进 QQ 动态」，或用户要求收紧 → 改为只给通用素材 | diary 依赖废弃 | 维持现状（观察中） |
| R19 | 创作层取材溯源清单 | `pending_events` 条目结构 | 预留 | 生活片段默认按**通用**投放（无 `source_uid`），但保留本次取材来源清单，供批 4 的**学习输入掩码**（ADR-0004 §3）追溯 | 批 4 实现学习输入掩码 | 若确认永不掩码 → 删 | 批 1 落结构 |
| R20 | 存量历史数据隔离豁免 | chronicle 表存量 `life`(74) / `daily`(3) | 临时 | 存量条目无法可靠反推来源 → **不打 `source_uid`**，只对 `kind=diary` 做隔离开关。<br>**用户 2026-09-25 裁决：内测期涉私原文影响不大，只做隔离开关即可**（后续换人设清空记忆做新一轮测试） | 已启用 | **换人设/清库后本条自动失效** | 批 1 生效 |
| R21 | chronicle 表列级迁移通道 | `services/store.py` | 待观察 | **首次**给 store 建立迁移机制（此前只有 `CREATE TABLE IF NOT EXISTS`，无任何 ALTER 能力） | 批 1 加 `source_uid`/`audience` 列即启用 | 并入 R12 的统一迁移器 → 本条归档 | ✅ 批 1 建立（`_migrate()`：`PRAGMA table_info` 探测 + ALTER，幂等；存量 diary 补标） |
| R22 | tier→详略档的长度区间 | `services/state/engine.py` `_FRAGMENT_TIER_LENGTH` | 待观察 | tier 由「资格门」改为「详略档」后，长度区间成为唯一调控旋钮（flat/minor 40~90、normal 80~180、major 200~400） | 批 1（C4）改造后启用 | — | 批 1 观察（参数见调参表 P1） |
| R23 | 由头取材「与最近对话不撞车」的文本重叠阈值 | `services/proactive/sourcing.py` | 待观察 | 取材优先级＝「未用过（bysource:used 去重）+ 与最近对话不撞车」的轻量判定阈值 | 批 1（C4）启用 | — | ✅ 批 1 实现（`_OVERLAP_REJECT=0.5`，字符 bigram Jaccard；参数见调参表 P2） |
| R24 | 指标 4-A 轨（快照状态组合熵） | `services/state/snapshot.py` | 待观察 | 注入侧多样性；与 B 轨对照。**A 变 B 不变 ＝ 只长数字不长故事** | 批 1（C5）埋点 | — | ✅ 批 1 实现（`state_diversity` 组合熵 + `output_diversity` n-gram 去重率，同一次产出同时采样） |
| R25 | 通信事实标记 | 创作合约 + 注入块 | 🟡 **创作合约 ✅ / 注入清单 ⏸ 留收尾** | ADR-0003 §8：通信事实以宿主账本为准；生活片段里「已发送」只写意图；注入块可带最近真实已发送清单 | 批 4（执行路线批 4 步骤 8） | — | **批 4 完成合约句**（E8(b) 裁定）：`creation/life.py::COMM_FACT_RULE` 明文「通信事实以真实记录为准，联系只能写打算/念头，不得写成既成事实」，接入 life 片段四档 + 起床补一段 + 编年史日结。**注入块的真实已发送清单⏸ 未做**（需接宿主账本，按 E8(b) 留收尾里程碑）；测试 `test_comm_fact.py::test_no_sent_list_injection_yet` 把这条边界钉住（将来实现了会红 → 提醒同步本行） |
| R26 | 文学授权段（与调制段分离） | `render/replyer_block.py` 注入块 | ✅ **批 3 启用** | ADR-0003 §6：铺陈/文学授权段**只增不删**；禁 Do-not 禁令组；字数类措辞用区间不用「上限」 | 批 3 启用 | — | **批 3 启用**：`AUTHORIZATION_TEXT` 写死代码（不做配置，防顺手精简）；边界＝**叙事授权**（铺陈/节奏/深度），**不越界人格约束**；回归护栏 = `test_replyer_block.py::test_authorization_always_present` + 回放台 `style_injection`（72 组合穷举） |
| R27 | 反证 / 重复提案数值（−0.2 / +0.05） | `state/continuity.py` | ✅ **批 4 落地保留原值** | ADR-0002 决策 3；与 R4/R5 同批由回放台定标 | 批 4 | — | **批 4 落地**：反证 −0.2 与合并 +0.05 均实现（`apply_refutations` / `merge_confidence`）；口径要点——反证按 **proposal id 显式引用**（不做文本相似度，HDSI 3.3 字面归并器漏洞），**异路径引用不生效**，扣信后**仍 ≥ 门槛则保持 pending**（只降信不驳回）；合并**只对新增场景加成**（复述不加信，HDSI 2.1）。用例：`test_promotion_engine.py`（29） |
| R28 | 慢变读取端投影条数上限（2 条软倾向） | `render/planner_block.py` | ✅ **批 4 落地（P8 定值 2）** | ADR-0002 §5：防晋升后写死；措辞「是倾向，不是必需反应，不是不变身份」 | 批 4 | — | **批 4 落地**：`build_slow_tendencies` 最多 `[promotion].projection_limit` 条（默认 2），相关度＝字符 bigram Jaccard（复用批 1 `sourcing.overlap_ratio`），同分保持登记顺序；**受众 fail-closed**（general 全可见 / per_user 仅归属人）；注入前过 `filter_guarded_entries`；关系阶段刻意排除（专门的关系行已渲染）。段首授权声明三连否定缺一不可。用例：`test_slow_projection.py`（19） |
| R29 | 脏数据读取降权权重 | chronicle / 晋升历史读取端 | 待观察 | ADR-0002 脏数据政策：保留 + 降权，**永不回溯清洗**（清洗会销毁「污染如何发生」的现场） | 批 2 起生效 | — | **批 2 引入**（读数入口已就位；权重初值见 P9，批 4 随晋升读取端接线定值） |
| R30 | 双向守卫关键词自动抽取规则 | `state/continuity.py` | 待观察 | 从 `[identity].world_rules/values` 切子句 + 剥祈使前缀（`不可说谎`→`说谎`）抽可命中片段 | 批 2-C6 启用 | 误伤率高于漏网率 → 改为纯手工 | **批 2 引入**（实现 + 双向调用点；词表与切分规则见 P13） |
| R31 | 互动配对（反馈 msg_id → 已送达回应 msg_id） | `services/learning/pairs.py` + `store.interaction_pairs` 表 + `plugin.py`（两个 hook 落点） | 🟡 **存储 ✅ 建 / 消费 ⏸ 不接** | 服务批 5 的 relationship LLM 提案；对应 ADR-0002 证据纪律第 4 条「关系晋升必须带反馈-送达配对」 | 批 4-C2 建 / 批 5 消费 | 实机数据累积后消费为空 → 查配对键是否错取（只认 `message_id`/`msg_id`，**不认通用 `id`**） | **批 4 只建不消费**（用户裁定：配对语义本批冻结）——⚠️ **本批冻结的是配对语义的读取与消费，不是事件级正向信号**（后者由 telemetry 共用实现，见 `positive_signal_days`）。三条落地保障：① 入站 pending 槽 + 出站配对照建（`handle_inbound_message` / `handle_post_send`）；② **晋升链路禁止 import pairs**——由 `test_pairs.py` 的 AST 断言守住（单实现纪律）；③ 配对失败静默吞掉（绝不能反噬发送链路）。用例：`test_pairs.py`（15） |

## 已处决项（非预留，防复活）

| 项 | 处决 | 依据 |
|---|---|---|
| `habits` / `shared_secrets` / `user_notes` / `events.declared` | **删除**（批 2） | ADR-0002 §8；全量架构解析报告 §3.7 实测零实现 |
| `events.declared` 列 | **真删**（`ALTER TABLE events DROP COLUMN declared`） | E12 裁决：不留兼容冗余；SQLite 3.50.4 支持 DROP COLUMN（已实测） |
| 插件内部 `state.focus.hot_thread`（读写与 status 展示） | **删除**（批 2） | 该字段自 v0.1.3 起无写入点；"心头事"改由创作层/生活片段独占 |
| `slow_change_log.jsonl` 方案 | **作废**，留痕走 sqlite `promotions` 表 + chronicle `kind=promotion` | ADR-0002 §4 |
| 旧规则关系线 `stage/familiarity/trust`（只进不退） | **废弃**，晋升线全量替换；`_STAGE_THRESHOLDS` 与 `record_branch_feedback` 的写关系值逻辑已删 | ADR-0002 §2 |
| 宿主 `reply_style` 迁移/清空方案 | **作废**，改增量调制叠加 | ADR-0003 §5 |
| 存量 `life`/`daily` 打 `source_uid` 反推方案 | **不采纳**（无法可靠反推），改为隔离开关 + R20 豁免 | 用户 2026-09-25 裁决 |
| `[creator_model]` 插件 HTTP 直连 + 明文 `api_key` | **删除**（批 1，R10 退役）；密钥一律由宿主 `model_config.toml` 持有 | 宿主 1.2.5 已透传 `model_name`（2026-09-25 容器核实） |
| 插件侧做「慢变/晋升」之外的 LLM 风格提炼 | **一期不做**（ADR-0003，HDSI beta8 事故实证） | 原文连续性已够，提炼会引入不可控风格漂移 |
| `services/anchor/` 新顶层目录（C6 原落点候选） | **否决**，守卫实现落 `state/continuity.py` | ADR-0001 §3 定稿布局无 `anchor/`；新增顶层目录必须先修 ADR |

## 跨插件契约字段（历史记录，非预留）

> 本节的「冻结」决定已于 **2026-09-26 由用户裁定推翻**，字段已双侧删除。留痕而非直接删节，
> 是为了避免后来者翻到历史记录时误判为「代理人擅自删除契约字段」。

| 字段 | 对端 | 状态 | 说明 |
|---|---|---|---|
| `narrative_diary_context` 的 `hot_thread` | `glcoge-mai-diary/services/diary/prompts.py`（原 `:53-55`） | ❌ **已删除（2026-09-26 双侧 lockstep）** | 原为「冻结保留」：对端真读它并渲染「心里挂着：…」进日记 prompt；当时裁决依据是「单侧删除属破坏性 API 变更、会让对端静默降级」。**2026-09-26 用户裁定推翻该冻结并直接删除**——因为改为**双侧同轮修改**后，「静默降级」这一唯一立论即失效。本插件输出点（`plugin.py` 的 `narrative_diary_context`）与 diary 读取点同轮删除。<br>✅ 零行为变化：该字段自 v0.1.3 起恒为 `""`，「心里挂着」分支从不触发 → 删前删后 diary prompt 逐字节相同。<br>✅ 部署顺序无关：对端读取已改用无该键的路径，缺失键不会报错。 |

---

## 调参观察点汇总（❗ 所有凭数据定标的数值集中在此，勿散落在代码注释里）

| 编号 | 参数 | 当前值 | 定标批次 | 定标手段 | 关联 |
|---|---|---|---|---|---|
| **P1** | tier 详略档长度区间 | flat/minor 40~90、normal 80~180、major 200~400 | 批 1 观察 / 批 4 固化 | 回放台产出长度分布 | R22 |
| **P2** | 由头文本重叠阈值（撞车判定） | **0.5**（字符 bigram Jaccard） | 批 1 观察 | 由头复用率 / 撞车率 | R23 |
| **P3** | 晋升门槛 minor（置信/场景/天数/冷却） | **0.82 / 3 / 2 / 72h（批 4 定标保留）** | ✅ **批 4 已定标** | 回放台喂 22 天真实素材 | R4 |
| **P4** | 晋升门槛 major（默认关） | 0.95 / 2 场景（**保持默认关**：真实数据量下 minor 已是冷却受限，major 无新增信息） | ✅ **批 4 已定标** | 同上 | R4 |
| **P5** | 指标 4-B 的 n-gram 窗口 | n=3~5 字符 | 批 1 埋点后观察 | 与 A 轨对照 | R7 |
| **P6** | 反证扣减 | **−0.2（批 4 落地保留）** | ✅ **批 4 已落地** | 回放台/单测定标（反证按 id 引用，异路径不生效） | R27 |
| **P7** | 重复提案合并加成 | **+0.05（批 4 落地保留）** | ✅ **批 4 已落地** | 同上（只对**新增场景**加成，复述不加信） | R27 |
| **P8** | 慢变读取端投影条数 | **最多 2 条软倾向（定值）** | ✅ **批 4 已定值** | 注入对照样本 | R28 |
| **P9** | 脏数据读取降权权重 | 未定（批 2 引初值） | 批 2 起观察 | 人工抽审 | R29 |
| **P10** | 话题偏好外部加权系数（用户接话 vs 生活片段来源） | **接话 1.0 / 自身片段 0.4**（批 3 初值） | 批 3 起观察 | 回放/实机观察 | R16 |
| **P11** | 铺群闸门基线（指标 4-B ≥ ?×基线） | 无（等批 1 出基线） | **批 1 出基线值** | 回放台 26 天数据 | R8 |
| **P12** | 晋升计数器告警线（提案/晋升/反证/回滚四指标） | 无 | 批 1 建通道 / 批 4 写入 | 实机数据 | R17 |
| **P13** | 守卫关键词切分（长度上下限 / 祈使前缀剥离下限） | 长度 2~8 字；剥前缀后下限 **2** | 批 2 起观察 | 真机 WARN 日志里的命中片段（误伤 vs 漏网） | R30 |
| **P14** | 漂移调制表的精力分档阈值（4 档） | `0.72 / 0.40 / 0.20`（沿用 `mood_by_energy` 同款口径） | 批 3 起观察 | 实机聊天体感（「句子偏短」是否与状态相符） | R16 / ADR-0003 §1 |
| **P15** | 话题签名长度上限（`TOPIC_KEY_MAX_LEN`） | **12** 字 | 批 3 起观察 | 回放台话题榜：聚合过粗（误并不同话题）或过细（同话题分裂） | R16 |
| **P16** | relationship 确定性晋升曲线（E14 新增） | **起步 3 日 / 每 2 日 +0.05 / 封顶 0.8**（`relation_min_days` / `relation_days_per_step` / `relation_step` / `relation_max`） | ✅ **批 4 初值已用归档粗验** | 归档实测：19 天用户终值 0.45（不爆表）、2 天用户 0.0（不误触发）；实机后按分布调 | R4 / R5 |
| **P17** | 提案提炼间隔与失败退避 | `interval_hours=168` / `failure_backoff_hours=6` | 批 4 初值，实机观察 | 提案产出的周节奏是否合适；退避是否够长（防 5.3 重试风暴） | R4 |

---

## 本轮决策留痕（2026-09-25 批 1 开工前）

| 编号 | 决策 | 裁决 |
|---|---|---|
| **Q1** | `narrative_diary_context` 的输出口径（diary 下游是公开 QQ 动态） | **维持现状**（继续给 `recent_chronicle` 原文），后续视情况优化 → 登记 R18 |
| **Q2** | 生活片段 `pending_events` 的 `source_uid` 归属 | **默认通用**（无标签全员可用）；另保留取材溯源清单供批 4 掩码 → R19 |
| **Q3** | 存量 `life`/`daily` 是否补打 `source_uid` | **不补**，只做 diary 隔离开关（内测期影响不大，后续换人设清库自然消解）→ R20 |
| **Q4** | 回放台层级 | **静态回放**（直接调 `build_context_block`/`build_bysource` 断言输出文本）；仿真 E2E 留批 3 |
| **Q5** | `/narrative status` 脱敏力度 | **只显示计数，不显示 QQ 号**（消 N2：状态文本曾把 mode_user_ids 原文外发） |
| **Q6** | n-gram 参数 | n=3~5，去标点与空白后计算 → R7/P5 |
| **Q7** | `tools/` 是否排除出插件打包 | 暂不处理（只增几 KB） |
| **Q8** | `2111957354`（用户本人）是否从泄露断言中移除 | **保留**——虽是本人，但与真实泄露同源，作为同源对照组 |
| **D1** | B 轨多样性算法 | 字符 n-gram 起步（同 Q6） |
| **D2** | `narrative_chronicle_append` audience 参数 | `audience: str \| None = None`，`None` → 推导 `source=diary`（R13） |
| **D3** | `hot_thread` 批 1 是否处理 | **不动**——已查实无任何写入点，无泄露面，归批 2 处决 |
| **D4** | `world_view`/`life_goals` 的 audience 规则 | 批 1 在 `audience.py` 规则表**预留条目并标注「批 4 启用」**，结构现在定好防返工 |
| **D7** | diary 依赖方向 | **架构裁定：diary 是 narrative 的后置附属，依赖方向 `diary → narrative` 单向**；diary 生成物完整隔离，narrative 处理不依赖它 |
| **D9** | 回放用例范围 | 扩到**三个目标会话全 0 命中**（`927386371` / `3401922770` / `2111957354`），fixture 已现成 |
| **D10** | `/narrative status` 泄露 | 批 1 顺手脱敏（同 Q5） |

> 补充事实（2026-09-25 数据坐实，ADR-0004 记载不全）：泄露受害面是 **3 个会话**而非 1 个；
> 首次泄露发生在 **09-21**（素材产生后不到 12 小时），且 09-21 12:49 真实用户 `3401922770`
> 当场反问「挪小黑是什么」——属已发生的用户侧困惑事故。回放台据此建三目标断言。

---

## 本轮决策留痕（2026-09-26 批 2 开工前）

| 编号 | 决策 | 裁决 |
|---|---|---|
| **E1** | 旧关系规则线（familiarity/trust/stage 只进不退） | **删旧规则线 + 加只读事实推导的过渡 stage**（`continuity.current_relationship_stage`，保离线回放确定性；批 4 换晋升值，切换点干净） |
| **E2** | 批 2→批 4 空窗期关系呈现退化程度 | **接受**：只有 stage 标签 + 最初见面；status 与注入块**不再显示熟悉/信任数字**（那是只进不退的假演化） |
| **E3** | `schema_version` 载体 | **复用 state 的 `meta.version`**（1→2）并让它**真正被读取**；不另起 kv 键（避 F2 全清坑） |
| **E4** | 开库发现旧版本 state | **WARN + 原地重置**（chronicle 保留）；静默重置会让人困惑 |
| **E5** | R14 启动期 LLM 一致性比对本批做吗 | **做，配置项 `[anchor].consistency_check` 默认关**（通道建好、默认不跑） |
| **E6** | 双向守卫关键词来源 | **自动抽取（world_rules/values）+ `[identity].guard_fragments` + `[anchor].guard_keywords` 手工补充**三路合并 → R30 |
| **E7** | 守卫命中后怎么办 | **入库前：丢弃整条产出 + WARN（不改写）；注入前：只丢该条目（不丢整段）** |
| **E8** | `proposals`/`promotions` 两表本批建吗 | **建**（schema 一次做完；与批 1 的 csv 计数器并存不冲突） |
| **E9** | 创作方法拆到 `creation/` 本批做吗 | **做，拆两笔**：纯移动（零行为 diff）+ 守卫接进创作出口 |
| **E10** | 回放台批 2 闸门用例 | **加**（`evidence_counting` + `scene_mapping_digest`） |
| **E11** | diary API 的 `hot_thread` 字段删吗 | ❗ **改判为「不删」**——前置检查发现 diary 侧 `prompts.py:53-55` **真读进 prompt** → 保留为常态空串，见上文「跨插件契约字段」 |
| **E11-终** | 同上（2026-09-26 用户最终裁定，晚于 E11） | ✅ **推翻「不删」→ 删除**：双侧 lockstep 同步改（narrative 输出点 + diary 读取点同轮删，diary 侧版本 1.4.2→1.4.3），原「破坏性/静默降级」立论随之失效。该字段恒空 → **零行为变化**。见上文「跨插件契约字段」节 |
| **E12** | `events.declared` 列怎么删 | **`ALTER TABLE ... DROP COLUMN` 真删**（实测 SQLite 3.50.4 支持） |
| **E13** | 「测试数只增不减」遇删功能怎么算 | **口径改**：不因重构掉测试；只有随功能一起删除的测试允许减少，且必须在提交信息里逐条列出 |
| **C6 落点** | 守卫实现落哪 | ❌ 否决 `services/anchor/guard.py`（违反 ADR-0001 §3 定稿布局）→ ✅ 落 `state/continuity.py`（与锚定白名单同文件），调用点随 C7 落 `creation/life.py` 出口 |

## 本轮决策留痕（2026-09-26 批 3 开工前）

| # | 议题 | 裁定 |
|---|---|---|
| **E1** | 仿真 E2E 无真服务器 | **合同级 E2E 作批 3 闸门**（载荷字段逐字对齐宿主 spec）；❗ **硬条件**：真 WS 灌帧 + 本地全栈**不是取消，是移回「批收尾里程碑」**（ADR-0005 原计划）→ 登记进执行路线附注，防「降级」悄悄变成「消失」（**已落** `docs/重构执行路线-v0.2.0.md`） |
| **E2** | R15 慢变 seed 归批 3 还是批 4 | **归批 4**；批 3 只留槽 |
| **E3** | 话题偏好证据源与话题表示 | **(a)** 主动消息发时记 pending 话题 → 用户接话 +1.0（P10）；生活片段自身主题 +0.4（降权） |
| **E4** | 注入位置 | **append 末尾**（REQUEST_CONTEXT 校验最安全；且变化字段后置不击穿前缀缓存） |
| **E5** | `[learned]` 声明进 `config.py` 吗 | ❌ **不声明**（ADR-0002 决策 6）：声明 → WebUI 表单以 **stale 值整体回写** → 冲掉批 4 写入的学习成果。改为 config.toml 原始区块 + tomlkit 直读写 + status 展示 |
| **E6** | 调制段含不含「句长」 | ✅ **含**（软表述）：漂移层首要维度即「句长语气」（ADR-0002 三层表 / ADR-0003 开篇）。**排除**的只是硬格式指令（拆气泡条数 / 句数 / 模板结构）；ChatRhythm 废弃的是**宿主检测器 + 硬格式指令**，不是软措辞 |
| **E7** | 耗时埋点 + 超预算 WARN | **加**（`_DRIFT_BUDGET_MS = 500`）：6 秒硬超时下唯一预警手段 |
| **E8** | `/narrative status` 暴露注入计数 | **加**：宿主 hook 异常全吞 → 必须自证「注入有没有上线」 |
| **E9** | 两个注入块分工 | planner 出生活内容 / 由头；replyer 只出调制段 / 关系语境 / 授权段 / `[learned]` 槽；**作息相位只作映射输入，不输出时间名词** |
| **E10** | 是否修订 ADR-0003 | **不修**：机制未变，实现细节入代码注释 + 登记表 + 执行方案 |
| **R-A 前提纠正** | 「服务器下线 → 真镜像 SDK 无法核对」 | ❌ **前提错误**：批 1 R10 结案（镜像 1.2.5 / SDK 2.8.1）靠本地 `docker pull`，与服务器在否无关。**已按此核对**：SDK 2.8.0→2.8.1 差异**仅 2 个文件**（`__init__.py` 版本号 + `capabilities/llm.py` 新增 `task_name`/`model_name` 参数），H1~H10 宿主契约逐条一致 |
| **C6 性质边界** | 授权段文案边界 | **只停叙事授权**（铺陈 / 节奏 / 深度），**不越界人格约束**（0944cae 回退铁律仍生效） |

### 布局补充（ADR-0001 §3 未列，批 3 新增；**不涉新增顶层目录**）

| 文件 | 归属 | 理由 |
|---|---|---|
| `services/learning/projection.py` | `learning/` | `[learned]` 区块 tomlkit 读写 + 防重入；`learning/__init__.py` 早已声明本目录为批 3/4 归属 |
| `services/learning/topic.py` | `learning/` | 话题偏好属漂移层（ADR-0002 三层表）；与 `drift_style.py` 分文件，避免单文件承担两种职责 |

---

## 本轮决策留痕（2026-09-26 批 4 开工前）

> 方案原件：`.workbuddy/批4执行方案.md`（v2）。以下 E1~E14 中 E1 为**用户强约束裁定**，
> 其余为规划期推荐项（用户「其余按你推荐」一次性拍板）。E13/E14 是 v2 因强约束 1 新引入的调参点。

| # | 议题 | 裁定 |
|---|---|---|
| **E1** | 本批冻结什么 | **冻结「配对语义」（反馈 msg_id → 已送达回应 msg_id 的读取与消费），不冻结事件级正向信号**。两类正向信号本批都到位：① 主动消息承接（`proactive_replied`）② 用户主动发起（`user_initiated_freq`）——**均按「用户 × 事件级 ts」落盘**，按 (uid, 日) 去重即可 → **零新增表**（不建 `positive_events`）。施工走两步：先核事实（已完成，见 §0.4），不足才补表（**未触发**） |
| **E1-a** | 强约束 ①：只吃正面信号 | 确定性晋升的证据**只允许**「用户主动发起 + 主动消息承接」；把互动总数/吵架计数当亲密证据 = HDSI「19:39 抗议」事故的确定性版本。`interaction_count` 明确排除 |
| **E1-b** | 强约束 ②：boundaries 冻结 | `boundaries` 本批**不自动晋升**，停在锚定值 0.0——边界感的移动需要读懂「抗议/舒适」的交互质量，计数推不出来。常量断言 `RELATIONSHIP_AUTO_PROMOTE == ("trust","closeness")` + 高分场景回归锁死 |
| **E1-c** | 强约束 ③：配对只建不消费 | 配对存储照建（服务器恢复后数据即开始累积，批 5 直接有米下锅），晋升通道先不接 → 登记 **R31**（存储 ✅ / 消费 ⏸）。只建不消费 = 零风险 + 零恒空，比「要不要建」的争论便宜 |
| **E1-d** | 掩码口径纠偏（用户） | ❌ 原「`world_view`/`life_goals` 只吃 `source_uid` 空条目」的口径会把 `kind=promotion` 留痕放进来 → 拿 AI 自己的晋升历史当晋升证据 = 自我强化循环。✅ 改为**白名单**：`EVIDENCE_ELIGIBLE_KINDS == GENERAL_KINDS = {life, daily}`，**promotion/diary 代码级禁入** + 同步断言测试。diary 此前靠 `SOURCE_DIARY` 巧合挡住，白名单才闭环 |
| **E1-e** | 附加禁令 A | **融合值不得当晋升证据**（`urge_factor` 一类被揉过的一维状态）。证据必须事件级/原文级 → `positive_signal_days` **只数行数、从不读取 value**（`proactive_replied` 的 value 语义跨版本变过，读它就会悄悄改变量纲） |
| **E1-f** | 附加禁令 B | 正向信号的**判定逻辑必须与 telemetry 共用同一份实现**（中文语义/单实现纪律）→ 回放台 `positive_signal` 用例直接喂归档 csv 给同一个 `positive_signal_days`，不复制判定 |
| **E2** | `[learned]` 投影范围 | **(b) 只投影 general 维度**（`world_view`/`life_goals`）。❗ relationship 是 per_user，**绝不进 config.toml**（写进去即跨用户可见 = 隐私事故），只留 db + 注入时按 audience 过滤 |
| **E3/E4/E6/E7** | 实现口径 | 全按推荐：E3 提案契约校验（path 白名单 / confidence clamp / 认知模式枚举）· E4 证据引用池内校验（编号→池映射，不信任模型）· E6 空提案也写 `last_ts` · E7 输入不足不调 LLM 且不写 `last_ts` |
| **E5** | `[learned]` 写回时机 | 晋升成功 / seed 成功后写回；**写回失败只 WARN**（db 才是事实源，投影可重建）；回滚走 `rebuild_projection`（会删陈旧键） |
| **E8** | 通信事实标记范围 | **(b) 只做合约句**；注入「最近真实已发送清单」需接宿主账本 → 留收尾里程碑（R25 已标 🟡） |
| **E9** | 回滚粒度 | **(a) 只回滚一步**。慢变区是「小步可动、须留痕」的，一次撤多步会让「哪一步被撤了」无法叙述（留痕链断裂）；要连撤就多调几次，每步各自留痕 |
| **E10** | 回滚命令门禁 | `/narrative rollback [路径\|last]`，`_is_admin` 门禁 + **慢变白名单校验**（非白名单路径直接拒绝——否则等于开了个任意写入后门） |
| **E11** | per_user 回滚怎么找归属 | 靠审计行 `source_uid`；**缺它则拒绝执行**（`missing_scope`）——宁可不动，也不猜是哪个用户 |
| **E12** | 提案失败退避是否持久化 | **不持久化**（只在内存，重启即清 → 重启后必再试一次）。失败指纹跨重启会让一次瞬时故障永久静音一个功能（HDSI 5.3） |
| **E13** | `relationship.stage` 怎么动 | **(a) 不独立晋升，仅由 trust+closeness 派生**（`derive_stage`）。受强约束 2 影响：boundaries 冻结后若 stage 仍按三维派生会卡住 |
| **E14** | relationship 晋升门槛与步长 | 引入**阶梯曲线** `relation_value`（达起步门槛即第一档，之后每档 +step，封顶 max）→ 登记 **P16**。初值 3 日 / 每 2 日 +0.05 / 封顶 0.8，已用归档粗验（19 天用户终值 0.45 不爆表，2 天用户不触发） |
| **E14-补** | 实现期发现（非规划项） | **提案值与审计值恒为字符串，但慢变维度里 `life_goals` 是列表、`trust`/`closeness` 是浮点** → 直接写回是静默数据损坏（列表被写成字符串，下一轮 `normalize_*` 再把它重置成空）。新增 `coerce_slow_value`（还原真实类型）+ `audit_text`（列表走 JSON 编码，`str()` 会给出单引号 `['a']` 导致回滚解析不回来），`apply` / `promote_relationship` / `rollback_last` 三处共用 |
| **收尾** | 闸门与里程碑 | 本批闸门 = `pytest ≥400` + **回放台 8/8**（实机测试因服务器未续期不在本批闸门内，用户已同意）。❗ **收尾里程碑三件套不得再顺延**：① 本地 Docker 全栈 ② 真 WS 灌帧仿真 E2E（批 3 E1 降级时承诺移交的）③ 真凭证白名单人工审 → 之后才 manifest bump + v0.2.0 |
