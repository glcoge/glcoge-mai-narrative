# 预留接口登记表

> 制度来源：ADR-0001 §7。`habits`/`shared_secrets`/`user_notes` 三个死字段的教训——没有登记制度的预留必然烂掉。
> 规则：**任何预留接口/字段/待观察参数必须在此登记**；代码处用 `# RESERVED(Rn)`（未实现预留）或 `# OBSERVE(Rn)`（已实现待观察）注释锚定；调用或删除前先查本表；每批收尾更新状态列。
> 类型：预留=声明了未实现 ｜ 待观察=已实现需定标/验证 ｜ 临时=明确将删。

| ID | 名称 | 位置 | 类型 | 用途 | 启用条件 | 删除条件 | 状态 |
|---|---|---|---|---|---|---|---|
| R1 | `[learned].style` 配置区块 + render 预留槽 | config.toml `[learned]`；`render/replyer_block.py` | 预留 | LLM 风格提炼产物的投影槽（v1 恒空） | 二期引入风格提炼（须齐 ADR-0003 四件套） | 观察期结束仍未启用 → v0.3 删除 | 批 3 落槽 |
| R2 | 群聊三轨接口 | `render/track.py` | 预留 | 寒暄接梗→表达库 / 剧本生活→漂移层 / 平时混合 的分类器挂点 | 用户宣布铺群解冻 | 群聊方案废弃 | ✅ 占位已建（批 0，冻结中） |
| R3 | 漂移风格 LLM 周度提炼 | `learning/drift_style.py` 二期分支 | 预留 | 原文连续性不足时补长期风格漂移 | 实机观察证明原文连续性不足 + 四件套齐上 | 决定永不做 → 删接口 | 未开工 |
| R4 | 晋升门槛数值：minor(0.82/3场景/2天/72h)、major(0.95/2场景，默认关) | config + `state/continuity.py` | 待观察 | 照抄 HDS 实测值，需回放台用 26 天真实数据定标 | 批 4 回放定标完成 → 固化为默认 | — | 批 4 定标 |
| R5 | 场景计数映射（1 生活片段=1 场景；1 互动日=1 场景） | `state/continuity.py` | 待观察 | 晋升证据的计数单位 | 同 R4（若永不触发晋升则改映射） | — | 批 4 验证 |
| R6 | 关系内部证据计数（原 familiarity 语义降级） | branch state | 待观察 | 晋升证据链的确定性时间戳来源 | 晋升机稳定消费它 | 一个月运行后晋升机不消费 → 删 | 批 2 引入 |
| R7 | 指标 4-B 产出多样性算法（embedding vs 字符 n-gram） | `state/snapshot.py`/telemetry | 待观察 | 铺群闸门的判据来源 | 批 1 两法对比后定型 | — | 批 1 选型 |
| R8 | 铺群闸门基线值（指标 4-B ≥ ?×基线） | 本表 + roadmap | 待观察 | 替代旧"指标4≥2x"的无数据判据 | 批 1 回放数据出基线 | — | 待批 1 |
| R9 | `is_command` 本地正则判定 | `plugin.py` 入站 hook | 临时 | 绕过宿主 after_process 时序 bug（命令文本进素材/刷关系值） | 已启用 | 宿主修复置位时序（跟踪 upstream）→ 删正则回读宿主标志 | 批 1 实现 |
| R10 | creator HTTP 直连兜底 | `creation/life.py` | 临时 | 宿主 1.2.0 吞 `model_name`（issue #2031）的绕行 | 已启用 | **✅ 已验证（2026-09-25 容器 1.2.5）**：`_resolve_llm_capability_route` 已透传 model_name（core.py:44-45/616-623）→ 批 1 切回宿主路由并删直连 | 删除已排期批 1 |
| R11 | 表达学习双阻断 hook（select/learn） | `plugin.py:445/465` | 待观察 | 防群腔调进私聊剧本、防剧本素材进表达库 | 已启用 | 群聊三轨实现时整体重估 | 保留 |
| R12 | `schema_version` + store 迁移入口 | `store.py` | 预留 | state 结构再演化时的版本化迁移挂点 | 下一次 schema 变更 | — | 批 2 引入 |
| R13 | diary append 接口 audience 参数 | `narrative_chronicle_append` API | 预留 | diary 侧未来打标推送的协议位（当前 diary 产物完全隔离，参数先收后忽略） | diary 仓适配 | diary 依赖废弃 | 批 1 加参 |
| R14 | 锚定一致性启动 WARN | `plugin.py` on_load | 待观察 | 宿主 personality vs 插件 world 双人格检测（LLM 一次性比对） | 已设计 | 误报率高到无用 → 降为手动命令 | 批 2 实现 |
| R15 | 慢变 seed 初始值（`origin=seed`） | `state/continuity.py` + config | 待观察 | 冷启动：world_view/life_goals 的晋升 diff 基线（ADR-0002「冷启动」） | 批 4 实现 | seed 长期未被任何晋升取代 → 查锚定派生质量或调派生 prompt | 批 4 引入 |
| R16 | 话题偏好外部加权系数（用户接话 vs 生活片段来源） | `learning/drift_style.py` | 待观察 | 防自主信息茧房（ADR-0003 §7） | 批 3 实现，系数凭回放/实机观察调 | — | 批 3 引入 |
| R17 | 晋升计数器四指标（提案/晋升/反证/回滚） | telemetry 通道 + `state/continuity.py` | 待观察 | 晋升频率真实数据（HDSI 盲区一年的空白）；告警线待实机数据定 | 批 1 建通道 / 批 4 写入 | — | 批 1 建通道 |

## 已处决项（非预留，防复活）

| 项 | 处决 | 依据 |
|---|---|---|
| `habits` / `shared_secrets` / `user_notes` / `hot_thread` / `events.declared` | **删除**（批 0/批 2） | ADR-0002 §8；全量架构解析报告 §3.7 实测零实现 |
| `slow_change_log.jsonl` 方案 | **作废**，留痕走 sqlite `promotions` 表 + chronicle `kind=promotion` | ADR-0002 §4 |
| 旧规则关系线 `stage/familiarity/trust`（只进不退） | **废弃**，晋升线全量替换 | ADR-0002 §2 |
| 宿主 `reply_style` 迁移/清空方案 | **作废**，改增量调制叠加 | ADR-0003 §5 |
