# 剧本人设系统（glcoge.mai-narrative）

让 bot 活在实时推进的剧本里：有生活、有情绪、会主动开口。本词汇表统一对话与代码中的领域语言；实现细节见 README 与设计文档。

## Language

### 状态机

**自我层（Self State）**：
bot 的全局生活状态——心情、精力、作息相位、心里挂念的事。全 bot 一份。
_Avoid_: 全局状态、主状态

**支线层（Branch State）**：
bot 与某一位具体用户的关系状态。v0.2.0 起关系线全量替换为慢变关系四维（信任/亲密度/边界感/阶段，由晋升机产生、可回退，见 ADR-0002）；旧规则线（熟悉度只进不退）已废弃。每用户一份。
_Avoid_: 用户状态、关系层、familiarity（旧规则线语义）

**锚定层（Identity）**：
用户手动配置、运行时只读的世界观与铁律（世界观/价值观底线/世界规则）。人设主体在主配置原生 `[personality]`，本层只补原生没有的维度。
_Avoid_: 人设配置（人设主体在原生）、角色卡

**慢变区（Slow Layer）**：
插件基于锚定层、跟随漂移层证据自主学习的长期演化维度——对世界的看法（world_view）、人生目标（life_goals）、长期关系（relationship 四维）。受控白名单字段，LLM 只能提案、宿主门槛审计后才能晋升，小步可动、须留痕、可反证回退。事实源在 sqlite，config `[learned]` 只是投影。
_Avoid_: 长期记忆（那是 A_Memorix 的域）、人设演化（易与锚定层混淆）

**漂移层（Drift Layer）**：
由自己的生活线驱动、随状态自然变化的表达与偏好维度——句长语气（规则实时调制）、话题偏好（确定性累积）、作息细节、情绪精力。文风长期演化靠原文连续性（生活片段/编年史注入），不做 LLM 风格提炼字段（ADR-0003）。
_Avoid_: 临时状态（漂移层含长期倾向）、风格学习（那是原生表达学习的域）

**世界书（Lorebook）**：
详细模式下承载世界观的长文条目集——每条含触发词与正文，存 data 目录、用户手写、运行时只读。仅详细模式启用。
_Avoid_: 知识库、设定集（口语可用，文档统一用世界书）

**简易模式（Simple Mode）**：
世界观来自锚定层一句话与分条的现状形态。
_Avoid_: 基础模式

**详细模式（Detailed Mode）**：
世界观来自世界书条目的形态；锚定层的世界观一句话不再注入，价值观底线与世界规则仍常驻。
_Avoid_: 世界书模式（口头语，文档统一用详细模式）

**里程碑（Milestone）**：
支线层里只进不退的关系事件记录（如阶段变更）。已写入的里程碑永不回退。v0.2.0 起定位为**只读事实字段**，不参与晋升；关系阶段本身可经晋升/反证回退，但事实记录不动（ADR-0002）。
_Avoid_: 事件（事件队列是另一物）

### 生活推进

**世界时钟（World Clock）**：
按固定间隔跑的纯规则 tick——精力向基线双向回归（含深夜睡眠恢复与互动提振）、心情映射、作息流转，零 LLM。
_Avoid_: 定时器、心跳、精力衰减（老版单调衰减语义，v0.1.5 起已废弃）

**作息相位（Routine Phase）**：
清晨/上午/午后/下午/晚间/深夜 六段标签，由剧本本地时间的小时确定。
_Avoid_: 时段（易与活跃窗口混淆）

**日照预期（Daylight Hint）**：
对"此刻窗外什么样"的感官描述（如"傍晚的光变得很软"），与作息相位互补的时间锚点。
_Avoid_: 天气（不模拟天气）

**剧本本地时间（Script Local Time）**：
按配置时区偏移推算的"bot 生活的时区"时间，全插件一切时间判断的唯一来源。
_Avoid_: 服务器时间、UTC

**创作层（Creation Layer）**：
用轻量模型把事件素材加工成"bot 自己的故事"的环节，是唯一日常 LLM 创作节点。
_Avoid_: 生成器、LLM 层

**生活片段（Life Fragment）**：
创作层的产出——一段第一人称的生活念头/小事/感慨，供对话引用与主动开口取材。
_Avoid_: 心情动态、hot_thread（已废弃该用法）

**编年史（Chronicle）**：
append-only 的 bot 生活史，每日小结与生活片段都写入，重置也不清空。
_Avoid_: 日志、历史记录

### 主动开口

**主动开口（Proactive）**：
bot 在活跃窗口内自己决定发起的消息，必须有由头，宁缺不干聊。
_Avoid_: 主动消息轰炸、推送

**由头（Bysource）**：
本次主动开口想说的具体事，取材自 bot 自身的生活（生活片段/里程碑/状态），禁止取材用户消息镜像。
_Avoid_: 话题、开场白

**活跃窗口（Active Window）**：
允许主动开口的钟点区间；窗口外与静默时段一律不发。
_Avoid_: 在线时段

### 会话

**剧本模式（Narrative Mode）**：
名单内用户（或名单会话）与 bot 的私聊对话形态——注入剧本状态、隔离表达学习。群聊不在模式内。
_Avoid_: 沉浸模式、RP 模式

**模式名单（Mode User IDs）**：
允许进入剧本模式的用户 QQ 白名单，是剧本模式的唯一准入来源。
_Avoid_: 好友列表

**表达学习隔离（Expression Isolation）**：
剧本模式会话不注入也不写入表达学习，防止群腔调稀释剧本人设。
_Avoid_: 屏蔽学习

**验收采样（Telemetry）**：
为验收仪表盘 5 指标做的旁路采样（主动频率/对话深度/回复率/多样性/成本），不影响主流程。
_Avoid_: 埋点（泛义）、日志

### 晋升与证据（v0.2.0，ADR-0002/0004）

**晋升机（Promotion Engine）**：
慢变区的唯一写入门——审计 LLM 提案的证据、置信度、场景数、冷却期，达标才落库并投影到 `[learned]`。状态存 sqlite `proposals`/`promotions` 两表。
_Avoid_: 学习器（歧义大）、进化引擎

**提案（Proposal）**：
压缩/提炼 LLM 对某个受控白名单维度（world_view/life_goals/relationship）提出的变更建议，带证据引用与置信度。提案不等于生效，只有晋升机审计通过才 applied。
_Avoid_: 补丁、草稿

**反证（Contradiction）**：
在可比情境下观察到相反行为的新证据，击中提案后置信度 −0.2 并 rejected。情绪或情境特例不算反证。匹配用受控枚举键（维度+场景条件），不用文本相似度。
_Avoid_: 回滚（回滚是 applied 之后的撤销动作）

**认知模式（Epistemic Mode）**：
提案证据的认识论标签——观察/转述/信念/提议/条件/确认，附认知主体。条件句不是承诺、信念不是事实；复述不增信。证据只锚原文条目，禁止以 AI 摘要或旧提案为证据（ADR-0002 证据纪律）。
_Avoid_: 类型（太泛）、标签（与受众标签混淆）

**慢变 seed（Seed）**：
上线时从锚定层派生的 world_view/life_goals 初始值（`origin=seed`），作为晋升 diff 的基线，与晋升值同受反证管辖。没有 seed，晋升机无现值可提案（冷启动死锁）。
_Avoid_: 默认值（那是配置语义，seed 是可演化的状态初值）

**场景（Scene，证据计数单位）**：
晋升证据的最小计数单位：1 个生活片段 = 1 场景；1 个互动日 = 1 场景。同一场景无论多少轮对话只计一次，反复叙述不加信。
_Avoid_: 回合、消息数

**原文连续性（Prose Continuity）**：
风格演化的主承载机制——模型接着自己生活片段/编年史的原文写，文风自然延续与渐变；不提炼风格描述字段（HDSI beta8 事故的镜鉴）。
_Avoid_: 文风学习

**规则调制（Rule Modulation）**：
确定性映射（mood/energy/相位 → 本轮表达提示），零 LLM，漂移层的实时通道。
_Avoid_: 风格生成

**受众隔离（Audience Isolation）**：
素材按 `source_uid`/`kind` 打标，注入与取材按当前会话受众过滤的四层强制（写入打标/读取过滤/学习输入掩码/创作层边界，ADR-0004）。diary 产物完全隔离——依赖方向是 diary→narrative 单向，narrative 绝不消费 diary 产物。
_Avoid_: 权限控制、群聊受众抽象（那是冻结中的 `u:/g:` 方案）

### 验收设施（v0.2.0，ADR-0005）

**回放台（Replay Harness）**：
`tools/replay/` 的正式验收工具——把归档 26 天插件数据 + MaiBot-export 真实聊天记录喂给晋升机/取材器/指标计算，离线验证重构效果与定标参数。
_Avoid_: 测试脚本（它是验收设施，进 git）

**仿真适配器（Simulated Adapter）**：
不连 QQ、按 NapCat WS 协议注入消息帧的封闭 E2E 环境；日常验收主通道，真凭证人工审只在批收尾。
_Avoid_: mock（泛指）

**预留接口（Reserved Interface）**：
声明未实现或实现待观察的接口/字段/参数，必须登记 `docs/预留接口登记表.md` 并在代码处 `# RESERVED(Rn)`/`# OBSERVE(Rn)` 锚定。不登记的预留一律视为死字段删除。
_Avoid_: 占位符、TODO
